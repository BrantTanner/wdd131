9/22
// Selecting elements
querySelector()
const element = document.querySelector("#myIdName");

querySelectorAll()
const elements = document.querySelectorAll(".myClassName")

// Content properties
.innerHTML
element.innerHTML = "<p>New content</p>";

.innerText
element.innerTExt = "New test content";

.textContent
element.textContent = "New text content";

.value
inputElement.value = "New input value";

let user input = inputElement.value

// Style Properies
.style
element.style.color = "blue"; 
element.style.backgroundColor = "yellow";
element.style.fontSize = "20px";

.myClassName
element.className = "newClass";

.add(), .remove(), .toggle()

// Create and Remove

.createElement()
const newParagraph = document.createElement("p");
new newParagraph.textContent = "This is new paragraph content.";

.appendChild()
const container = document.getElementById("container");
container.appendChild = 

// Attibute
.setAttribute()
element.setAttribute("src", "new-image.jpg");

.getAttribute()
let src = element

.removeAttributeNS()

9/24/25
const PI = 3.14;
let radius = 3;

let area = radius * radius * PI

console.log(area)

radius = 20;

area = radius * radius * PI;

console.log(area);

const one = 1;
const two = '2';

let result = one + two;
console.log(result);

result = one + Number(two);
console.log(result);

let course = "CSE13"; //global scope (works outside the curly braces)
if (true) {
    let student = "John"; //local scope (variable is inside the curly braces)
    console.log(course); //works because in braces
    console.log(student);//works because in braces
}
console.log(course); //works because is global scope
// console.log(student); //does not work since it's local scope
